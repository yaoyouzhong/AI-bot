# 更新日志

本文件记录项目的重要变更。

## 未发布

- 收尾公开源码与分发材料：核对迁移来源，限制私有/构建文件与未审核图片进入源码包，修正额度趋势说明；锁定当前固件依赖版本。
- 补齐微软 SDK 原始许可、官方 REDIST 清单与 DLL 哈希核对；Windows 候选附完整依赖声明、分发条款，固件材料包附对应源码、许可和重建配置，源码包附 SHA-256 清单。仍未推送、发布或完成产品级验收。

- 新增本机 Codex 额度历史和独立趋势窗口，不改变设备页面；处理重置、缺失采样和损坏历史。打开镜像时重读缺失的私有桌宠资源，不覆盖已有选择。

- 增加隔离 Windows 候选打包、NuGet 许可文本收集、安装说明、文件/ZIP 校验及解压后回归；随包只附三像素合成导入测试数据，不包含私有桌宠缓存。

- Windows 桥接改为 WinExe，去掉启动后关闭控制台的逻辑，消除终端闪窗；自动化诊断通过 dotnet 控制台宿主运行，保留输出和退出码检查。

- 新增空白用户目录的公开版本回归入口、隔离本地构建脚本、当前文件/历史基础内容检查，以及 Windows/macOS CI 检查；同步发布准备说明，未升版或发布。下方历史部署描述对应各功能当时的实施阶段，不代表当前运行状态。

- 补齐等待输入/完成确认、本机 Token、自动优先级/屏保唤醒、认证 Wi-Fi 资源缓存同步；加入回环 HTTP、CRC、状态与统计回归。音乐布局和天气秒字对齐旧版，修正默认桌宠预交换色序与 TFT 显示色序。
- 接入 macOS 图库、镜像、角色缓存/恢复、事件统计、轮播和对应 XCTest；Windows/固件仅本地验证，Mac 仍未编译验收，未替换稳定设备或发布。

- 独立实现 petdex 图库，沿用旧版搜索/预览/角色/动作布局；增加下载限额、列表缓存回退、Windows 原生 WebP 解码和分角色保存。真实 Boba 解码及九动作/两角色离线测试通过；未部署 USB 设备。

- APET v2 保留本机 Claude/Codex 默认桌宠原尺寸，增加固件独立资源槽、分角色导入/恢复菜单、重启持久化和上次选择备份。离线验证角色隔离及恢复资源版本变化；未部署设备。

### 页面数据解析与熟悉的托盘导航

- 迁入经过来源审计的维护者天气/国产额度授权代码、几何天气动画、机器人图标和合成完成提示音，接入独立 AI-bot 适配层；保持正在运行的旧桥接/固件不变。
- 恢复单/双额度页、厂商选择、轮播设置、倒计时、股票布局/翻页、系统曲线、GIF 多帧及 Windows 桌宠像素持久化；新增离线渲染、策略/缓存/生命周期/动画测试。默认精灵图和分状态选择仍未完整对齐。
- 新增隔离的旧版天气布局预览；未知命令直接退出，不启动桥接。新仓库的渲染已更新，未部署候选。

- 修复固件对只读 ArduinoJson 对象/数组使用错误类型判断，导致天气、股票、额度、系统和音乐数据被静默跳过的问题。
- 真机验收增加页面解析状态读回；另增使用生产运行时的真实数据检查。
- 恢复旧版托盘分组及左键开关镜像，与设备控制和 LAN 状态共用持久显示策略。

### 真机通信修复与旧版屏保对齐

- ESP8266 初始化串口前分配 8192 字节接收缓冲，保留同步绘屏和 Flash 写入期间到达的完整 JSON/资源帧。
- 新增真实设备资源、页面、亮度和 USB 超时验收，明确报告失败并恢复原模式与亮度。
- 固件与 Windows 镜像恢复旧版 204×76 青色数码屏保时钟、黄色冒号、日期/星期与五秒移动。

### 近期基线对齐

- 固件和 Windows 镜像展示每笔 Codex 重置额度到期日期，同日多笔保留、长列表翻页；桌宠页独立预留额度区域，避免动画覆盖。
- Windows/macOS 在额度数量不变但明细不可用时保留最近日期并标记陈旧。
- 余额数字与小字号币种按基线对齐；Windows 网卡列表缓存 30 秒，按网卡分别计算增量且采样不重入。
- 在 `docs/ALIGNMENT_2026-09-08.md` 记录旧仓库工作区的精确基线及剩余验收边界。

### 新功能

- 新增 Windows/macOS 不依赖 LAN 的 USB 设备信息与单独确认 Wi-Fi 重置，使用关联应答且不自动重试破坏性操作。
- 新增保持 USB 供电的暂停回退测试、设备 USB/LAN 计数、自动恢复，以及 Windows 真机/合成测试命令。
- 新增 USB 发现设备私网地址与 macOS 设备管理基础。

- 新增由 USB 配对下发令牌的认证 Wi-Fi 回退和独立时钟行为。
- 新增 Open-Meteo 天气、A/H/美股行情及最近成功缓存。
- 新增设备天气/股票页面、自动轮播、分页和 Windows 自动屏保控制。
- 新增 Claude/Codex 账户额度解析、最近成功缓存和设备额度页面。
- 新增阿里/Kimi/MiniMax/DeepSeek 额度解析、MiniMax API 刷新和设备总览页面。
- 新增 Windows CPU、内存、上下行速率实测和设备系统监控页面。
- 新增 Windows 媒体会话信息、播放进度、音乐页面和 AUTO 播放覆盖。
- 新增不依赖导入精灵图的原创建模桌宠 `BYTE SPROUT`。
- 自动屏保遇到 AI 开工或音乐开始时临时唤醒，并在用户输入后恢复原模式。
- 新增 COBS 二进制资源分块、逐块/整包 CRC32、ACK 重试和 LittleFS 校验后替换。
- 新增中文音乐文本与封面 RGB565 资源发送，并从 LittleFS 逐行渲染。
- 新增带许可文件门槛的运行时桌宠导入、RGB565 转换、USB 发送和设备渲染。
- 新增天气中文文本和分页股票名称预渲染资源，并保留代码降级显示。
- 新增独立 macOS 菜单栏、活动读取、Keychain 配对令牌和认证 LAN 状态基础。
- 新增九页面 240×240 Windows 镜像、合成 PNG 视觉自测和模式/亮度控制窗口。
- 新增阿里、Kimi、MiniMax 和 DeepSeek 仅显示额度字段的隔离 WebView2 登录捕获。
- 新增 Windows 设置对话框及天气、股票、屏保和首选串口的白名单持久化。
- 新增 macOS Open-Meteo 天气、A/H/美股、最近成功缓存和非敏感数据源设置。
- 新增 macOS 基于公共 Darwin/Mach API 的 CPU、物理内存和活动网卡流量采样。
- 新增 macOS Claude/Codex 账户额度解析、固定官方域名请求和仅显示缓存。
- 新增 macOS USB 串口探测、460800 波特率控制帧、Keychain 随机配对和 Wi-Fi 回退配置下发。
- 新增 macOS 设备页面、亮度和显式重发 Wi-Fi 回退配置菜单控制。
- 新增 macOS 按本机空闲时间进入屏保并恢复先前设备页面。
- 新增 macOS COBS/CRC32 二进制资源协议及逐块 ACK 重试。
- 新增 macOS 天气和股票标签去重版本化 RGB565 资源渲染。
- 新增带许可门槛、首帧缩放和 USB 发送的 macOS 桌宠导入。
- 新增默认关闭的 macOS Apple Music/Spotify 元数据、进度、中文文本发送及具备自动化权限声明的开发 App 包。
- 新增有大小与尺寸上限的 macOS 当前封面解码、112×112 RGB565 发送和旧封面清除。
- 新增 macOS 自动屏保在音乐开始或 AI 开工时的临时唤醒。

### 安全

- 设备接口只绑定一个私有网卡地址，并强制使用 DPAPI 保护的配对令牌。

## 0.1.0 - 2026-09-04

### 新功能

- 新增独立实现的 Windows 托盘桥接程序。
- 新增 ESP8266 USB 状态显示固件。
- 定义 `@AIBOT` version 1 单行协议。
- 增加持续集成与标签触发的发布框架。
