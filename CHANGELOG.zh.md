# 更新日志

本文件记录项目的重要变更。

## 0.4.0 - 2026-09-23

- 电脑短时繁忙导致状态中断时，小屏最多保留最近有效页面 30 秒；USB 失效 8 秒后仍开始 Wi-Fi 回退并如实报告离线，电脑明确退出时立即显示离线。
- 降低 Windows 后台活动扫描频率，减少系统繁忙时对串口心跳和状态更新的干扰。
- 笔记本网络地址变化时，Windows 桥接自动重绑与设备同网段的网卡监听；USB 可用时同步下发新地址。
- Windows 与 ESP8266 新增经配对令牌 HMAC 认证的局域网发现。设备或笔记本 IP 变化、USB 不可用时，设备可在当前局域网寻找桥接；只有通过带认证的状态请求才保存新地址。

Windows Release 与 ESP8266 固件构建通过。真机完成 5 分钟纯 Wi-Fi 状态传输、旧桥接端口失效后的自动发现、普通 Wi-Fi 回退及 USB 恢复；模拟测试覆盖两端地址同时变化。设备独立供电下的真实双端 DHCP 同时改址尚未验收。发现要求同网段广播可达，Mac 桥接暂不支持此自动发现。使用地址发现功能需同时更新 Windows 桥接与设备固件；Windows 安装包未签名，Apple Silicon 应用仍为临时签名且未经公证。

## 0.3.0 - 2026-09-21

- 刷机和备份仅暂停 USB，结束或刷机窗口异常断开后自动恢复；桥接进程和网络服务保持运行，不退出或重新启动任何桥接副本。精简 Windows 刷机窗口字号、间距、按钮状态和折叠详情。
- 刷机百分比每秒最多刷新一次并去除重复进度日志；备份连续 30 秒无输出时停止读取，允许在写入前降速重试。
- 修复备份百分比等待换行而不刷新的问题；刷机优先使用 460800 波特率，高速读取失败时在写入前自动降速重试。
- 刷机窗口自动选择已连接的单台 USB 设备，扫描异常保留设备身份并自动恢复；独立图标与设备状态卡片，安装包内置校验过的刷机工具，无需运行时下载。
- 自动识别已连接的单台 USB 设备，多台设备无法区分时才通过插拔定位；端口号收进折叠详情，Windows 排除蓝牙串口，设备身份变化时阻止继续刷写。
- 新增 Windows 与 Apple Silicon Mac 原生刷机窗口：自动识别设备，选择固件 ZIP/BIN 后使用内置且校验过的官方工具完成完整备份、写入和回读校验；刷机时释放 USB，结束后恢复连接。
- 下载、安装、刷机和首次连接步骤集中到一份完整图文指南。

Windows 此前已完成实机刷机及备份测速；本次仅暂停 USB 的交接逻辑已通过本地回归，实机验收待完成。新增 Mac 窗口已通过 macOS CI 测试和构建，设备验收仍待完成。本版已标记为正式发布；下述验收限制保持不变。Windows 安装器未签名；Mac 为 Apple Silicon、ad-hoc 签名且未公证。已使用 v0.2.2 固件的设备无需为本次更新重新刷机。

## 0.2.2 - 2026-09-21

- 修复完成/待输入提醒覆盖手动选择页面的问题，同步设备和镜像；自动模式仍保留提醒切页。设备诊断区分选择模式、有效模式与实际绘制页。

- Windows：改用港股实时行情，保留三位小数报价及指数精度；逐只补查缺失股票，失败时保留已有行情。
- Windows：切页请求不再阻塞界面；USB 资源传输在分块之间优先处理最新选择，避免排队中的旧心跳把页面切回。

手动切页修复需要同时更新桥接程序和 ESP8266 固件。Windows/设备行为已实机验证并由用户确认；macOS 同步显示优先级修复，交互验收仍待在 Mac 上完成。Windows 安装包未签名，Apple Silicon 应用为 ad-hoc 签名且未公证。

## 0.2.1 - 2026-09-17

### Windows 修复

- DeepSeek 余额查询遇到临时失败时自动重试一次，刷新间隔后重新建立连接；区分认证、访问限制、限流和网络错误，并保留最近成功余额。
- 额度告警先等待自动恢复；成功后取消待发送提醒，保留具体失败原因。
- 增加有数量限制且不含凭据的接口和网页采集诊断，区分登录跳转、采集超时及余额保存成功。
- 本机和局域网监听端口暂不可用时自动重试。持续冲突可通过现有 AIBOT_HTTP_PORT 配置其他端口，USB 配对同步下发给设备。

### 验证与限制

- Windows Release 构建、隔离公开自测和状态读取通过；已验证 DeepSeek 真实刷新、智谱重新登录后的后台余额刷新、USB 设备连接及自动循环设置保留。
- 智谱仍需要有效的内置网页登录，再次重启后的登录保留尚未验证；不会强行抢占其他进程持有的端口。
- macOS 与固件行为相较 v0.2.0 不变，本版不要求已正常运行 v0.2.0 的设备重新刷机；发布包按新版本重新构建。
- 测试版。Windows 安装器仍未签名；macOS 仅支持 Apple Silicon/macOS 13+，采用临时签名且未公证。此前记录的供应商和平台验收限制继续适用。

## 0.2.0 - 2026-09-16

- 补齐小米 MiMo 网页额度采集、严格解析、缓存、镜像与固件页面；国产额度后台检查和提醒仅面向当前选中且曾授权取数或配置凭据的厂商。小米真实套餐仍待验收。

- 新增阶跃开放平台余额和百度千帆指定模型资源包官方接口、凭据配置、失败缓存与设备可选字段；不接入云账号余额。真实账号、设备及 macOS 运行验收待完成，其余目录厂商保持待接入。

- 国产额度优先使用 DeepSeek/MiniMax 官方接口及 Kimi Code 官方本地服务 API；增加桥接内凭据设置、网页失效提醒、缓存提示及手动刷新。阿里 Token Plan、智谱余额保留网页方案，尚未确认对应公开 API。

- Windows/macOS 与 ESP8266 屏保增加农历日期，支持闰月和设备离线计算；缩小移动范围，避开底部 PC OFF 提示。实机及 macOS 运行验收待完成。

- 修复 macOS 13 农历代码编译兼容性，保留闰月显示。
- 优化国产模型设置窗口与高 DPI 厂商长名称显示，同步九张主页功能图及安装教程。

本版为测试版：包含 Windows 接入实现，新增厂商真实账号及实体设备验收仍待完成。macOS 包含农历和协议兼容，不包含新增的 Windows 凭据采集功能。设备农历及新增页面需要更新固件。Windows 安装器未签名，macOS 采用临时签名、未经公证。

## 0.1.3 - 2026-09-16

### Windows 安装

- 新增约 8 MB 的中文联网精简安装向导，自动检测 .NET 8 桌面运行时和 WebView2，已有组件跳过，缺少时自动下载补装。
- 显示 .NET 下载进度，支持取消与重试，校验固定 SHA-256 后才运行。创建快捷方式，卸载保留用户设置和公共运行环境。
- 发布包增加安装 EXE 及校验文件；编译后的下载测试覆盖正常响应、内容被修改、404，以及环境检测不触发下载。

### 图文教程

- 推荐下载入口前置，补充下载选择、应用安装、固件备份及刷写图解。ZIP 手动安装与源码构建单独说明。
- 同步中英文主页，功能图扩为两行三列共六张，并更新分发说明及验收标准。

本版为测试版。安装器未签名；全新电脑补装、真实窗口显示、升级与卸载尚未完成实测。缺少运行环境时需要联网；已有正常固件不用为此次安装程序更新重新刷机。

## 0.1.2 - 2026-09-16

Windows 维护测试版。升级时先退出桥接，完整解压新版安装包，并保留现有用户 AppData 数据。

### 额度趋势与窗口修复

- 不完整日期也展示可核实的每日额度增量，并标明记录范围；缺失时段不估算，不完整日期不计入全天日均。
- 容忍重置时间的小幅抖动，避免漏算正常用量或误报重置；保留对实际额度下降、周期变化和采集断档的检查。
- 历史保存临时失败后，下次采样自动重试；原历史文件不可读时仍保留原文件。
- 修复高 DPI 下镜像连接信息被遮挡，额度趋势窗口改为紧凑居中显示，优化表格列宽、图表数值标签和统计单位说明。
- 修复隐藏启动方式导致额度趋势首次点击不显示的问题，激活前核对 Windows 原生窗口可见状态。

### 其他 Windows 改进

- Windows 会话日志改为后台单任务扫描并发布缓存，避免慢扫描阻塞 USB 心跳、Wi-Fi 状态接口和托盘，导致小屏幕间歇显示 PC OFF；扫描失败保留最近成功数据，跳过未增长的生命周期日志，并提供仅本机可用的采集与设备诊断接口。

- 托盘右键菜单新增“关于 AI-bot”，显示当前运行程序版本、作者 yaoyouzhong & Codex 及可点击的 GitHub 仓库链接。

### 验证与限制

- 本地已验证 Windows 构建、状态读取、额度统计回归、175% 缩放布局和隐藏启动下首次打开的复现与修复；重启后真实额度采样已成功保存。
- 缺失的历史采样无法补回，每日已记录用量可能低于全天实际用量。
- macOS 和固件行为未变，随本版本重建安装包。Mac 仅支持 macOS 13+ Apple Silicon，采用临时签名、未经公证，真实 Mac 验收仍待完成；本次不宣称新增完整设备实测验收。
- Windows 需要 .NET 8 Desktop Runtime x64 和 Microsoft Edge WebView2 Runtime。安装包附带许可证声明和 SHA-256 校验文件。

## 0.1.1 - 2026-09-11

这是首次公开的 GitHub 测试版。0.1.0 为开发基线；本版包含此前累计的桌面端、固件与打包能力，以及本次 Windows 自启动修复。

### Windows 修复

- 改用当前用户登录后延迟 5 秒的交互式计划任务，避开普通 Run 启动队列。不保存密码、不要求管理员权限，允许电池供电且不限制运行时长。旧版启动项关闭再启用一次即可迁移，移动程序目录后需重新设置。
- 在初始化供应商、打开 USB／HTTP 连接前阻止重复桥接实例。
- 托盘悬停文字简化为应用名称与操作说明，页面内活动状态保持不变；正常启动不闪现控制台。

### 包含的能力

- Windows 托盘和镜像：Claude／Codex 活动、账户额度、本机 Token 统计、完成／待操作提醒与 Codex 额度趋势；国产授权覆盖阿里、Kimi、MiniMax、DeepSeek 和智谱。
- 天气、股票、系统监控、音乐、自动循环和屏保；供应商请求失败时保留最近成功数据。
- ESP8266 以 460800 波特率优先使用 USB，支持认证 Wi-Fi 回退、独立时钟、设备控制与确认式二进制资源传输；改进串口缓冲、页面数据解码和 RGB565 渲染。
- 原创 BYTE SPROUT 桌宠、按角色选择／导入动画和资源持久化；不分发私人账户数据及个人图片素材。
- Apple Silicon macOS 测试应用，提供菜单栏、镜像、活动／额度、天气、股票、系统监控、音乐、桌宠与设备控制，具体能力受平台限制。
- 中英文安装／刷机指南，以及附带许可声明和 SHA-256 校验的 Windows、Mac、源码与固件材料包。

### 验证与限制

- 本地已验证 Windows Release 编译、控制台宿主状态诊断、计划任务拉起和连续三次重复启动；循环页面、顺序及间隔保留。本轮尚未观察真实重启／登录触发及实际鼠标悬停效果。
- 公开前必须通过云端编译／测试和产物校验；自动化测试不代表真实账户或设备完整验收。
- Mac 需要 macOS 13+ 和 Apple Silicon，仅作 ad-hoc 临时签名，未作 Developer ID 签名或 Apple 公证；首次启动、权限及设备行为仍待真实 Mac 验证，本包不支持 Intel。
- 本次自启动修复验收未发现 ESP8266 USB 串口，完整逐页显示及 Wi-Fi 回退验收仍待完成。本版不改变协议版本。
- Windows 需要 .NET 8 Desktop Runtime x64 和 Microsoft Edge WebView2 Runtime。请完整解压 ZIP，升级前退出桥接并保留用户 AppData。

## 0.1.0 - 2026-09-04

### 新功能

- 新增独立实现的 Windows 托盘桥接程序。
- 新增 ESP8266 USB 状态显示固件。
- 定义 `@AIBOT` version 1 单行协议。
- 增加持续集成与标签触发的发布框架。
