# 2026-09-08 近期代码行为对齐

## 基线与来源

- 旧仓库 `esp8266-ai` HEAD：`2efab1d0661e24b926c0fef808522d427a0cd5d0`。
- 包含当天未提交的 `firmware/src/main.cpp`；核对时 SHA-256 为 `2E31F680CE495B2D88F02DF7260E2E35EB372448BE44FDCC5106A6FC611729CB`。
- 新仓库起点：`d55acfb35e4fb10cf5c814ae04e1ce230340b31a`，保留 USB 管理及保持供电的回退测试。
- 旧仓库只读，未复制旧源码、资源或历史。下列行为在新架构中独立实现；不是 cherry-pick 或逐行镜像。

## 核对结果

| 近期旧版行为 | 新版处理 |
| --- | --- |
| `2efab1d` 每笔重置额度到期信息 | Windows/macOS 原有解析和传输数组已核对；补齐固件与 Windows 镜像的逐笔日期，同日多笔保留；未知日期 `--`，多于两行按四秒翻页 |
| 到期明细临时缺失时保留缓存 | Windows/macOS 在正数总量未变时保留最近日期并标记 stale；总量改变后不套用旧明细 |
| 双状态页显示额度总数 | 固件与 Windows 镜像增加 `R*N` 总数 |
| 当天未提交的桌宠绘制保护 | 使用独立底部额度区；112×112 导入图和原创建模动画均在其上方；动画帧不清除/重绘未变化的额度区，页面切换和离线恢复重新绘制。保留活动来源标签，不移植旧精灵裁剪实现 |
| `6d3315b` / `394810f` 余额数字与币种排版 | 固件与 Windows 镜像分离数字和币种，按字体基线对齐，币种使用较小字号 |
| `918c061` 网络采样开销 | 保留新版一秒采样周期；网卡列表缓存三十秒，失败也限频，排除常见虚拟/无单播地址接口；采样防重入，单调时钟，按网卡增量防止网卡增删制造流量尖峰 |
| `0daaead` MiniMax 时间/百分比语义 | 新版已有毫秒剩余时间换算、区间与周额度解析，保留现有链路 |

## 验证及限制

- Windows Release 构建、数据源合成测试、实际 CPU/内存/网络采样、九页镜像渲染、LAN 自测、USB 管理合成测试及 `--status-once`。
- 固件 PlatformIO 编译；未刷机。字体、翻页及连续桌宠动画仍需实际屏幕复核。
- 新增 Windows 测试覆盖同日多笔、时区跨日、未知日期、零额度、明细缓存及网卡增删/计数归零。macOS 增加相应解析/缓存测试源码，但本机无 Swift 工具链，未执行。
- 这是对上述近期变化的行为核对，不是所有历史功能的完整等价证明。真实账号、硬件回归及 macOS 平台缺口仍以 `FUNCTIONAL_PARITY.md` 为准；旧版真机通过不代替新版验收。
- 不自动同步后续旧仓库修改；不推送、发布、升级版本或更改设备配网。
