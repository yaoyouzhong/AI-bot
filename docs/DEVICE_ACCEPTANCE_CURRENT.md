# 当前真机验收

> 历史验收记录：下列候选哈希、进程号和等待操作只适用于记录当时，不是当前运行状态或操作指令。当前发布边界见 [发布前检查](RELEASE_READINESS.md)。

用户授权：先验收，发现问题再修改，不急着恢复旧版。本轮不得因普通测试失败自动回滚；旧备份保留备用，不删除。

## 本次候选

- 固件 `firmware/.pio/build/nodemcuv2/firmware.bin`，515712 bytes，SHA-256 `3FE0BC6EAFE4477D88773DD58B9913CD04E259D1012D8BF1C5E929EDA4A20814`。
- Windows DLL SHA-256 `B2A90A3D52FA2F3CA5FB379FDFA2CDDFC9A0977DB3751F5F9A509974054CF00B`。
- 现场 CH340 COM7 在线；旧版进程 PID 55568，等待托盘正常退出释放串口。
- 旧完整备份校验 `7EBDD0F8F9EEBB6805111A96805D6564E66BB049E0096422D9B88682FF5C1ACD` 正确。

## 验收顺序与状态

1. 释放串口并刷入候选：待执行。
2. 合成资源 ACK、模式/亮度/USB 超时恢复：待执行；合成画面不是最终视觉基准。
3. 生产数据及两只桌宠、两个图标资源 ACK：待执行。
4. 保持供电暂停 USB，检查 LAN 帧增长和 USB 恢复：待执行。失败时记录实际状态，不直接归因为网络隔离，不改防火墙或清空配网。
5. 启动新版桥接，逐页实体视觉、动画、交互与音乐：待执行。

macOS 独立验收，不由 Windows 构建或 USB 测试代替。未推送、未发布。

## 已执行与现场反馈

首候选刷写校验成功。九模式、四类合成资源、亮度及 USB 超时恢复通过。生产数据回读天气 26°C、自选股 6 条、Claude/Codex/四家国产额度及系统字段均存在；两只桌宠和两个图标 ACK 通过。音乐无会话，不算验收通过。

Wi-Fi 回退失败：暂停前 USB 79 / LAN 0，暂停期间 USB 79 / LAN 0、桥接离线，恢复后 USB 80 / LAN 0；USB 恢复通过。保留新版继续排查，未恢复旧固件。

用户实体反馈：股票汉字偏小；Codex 应是抱电脑机器人而不是内置黄色宠物；Codex 刷新太闪。三项均按真实验收失败处理，不以先前镜像测试否定反馈。

### 已实施修复，待用户再次看屏

- 从旧完整备份按 eagle.flash.4m1m.ld 提取 LittleFS（只读源文件），发现实际自定义 `/x.bin`：120×120、6 帧、120ms。迁入本机 Codex 选择槽，原默认资源保留。此前默认头文件像素一致不代表设备实际选择一致，原结论范围有误。
- 解码备份 `/stock-ui.rle` 并逐像素比对。设备目标字形为 21px；固定像素字号，避免验收 CLI 的 DPI 上下文令 9pt 变小。主线程、后台线程、未初始化 DPI 的独立进程比较均为 0 / 62400 差异。股票价格/涨跌行恢复 y+22。
- 单额度页仅在显示内容或页面/资源代次改变时完整绘制，避免每秒清屏；不变时只更新宠物。断线恢复、切页和资源替换会正确失效缓存。
- 修复固件 516960 bytes 已刷入且 Hash verified，RAM 42184，Flash 512803。Windows 再次 Release 编译成功。

提取的原设备文件（可能含设备设置）与图像保留在仓库外的本地备份目录，禁止公开提交。源备份未改动。

### 修复部署与重测结果

- 固件最终 SHA-256 `DA87C1D1833B98F8E4F015AEED70CB6F02AF7AFE55CDC845BCA02AE456F414FB`；Windows DLL `7CBEC5F25311B2DE823EC94403900B21538034F79E7FFAC50D8BC4956FEEA18E`。
- Codex 传输连续失败，隔离测试资源发送者后定位 sequence=223（172824 bytes）。只读保存当前完整 LittleFS 到仓库外 `candidate-fs-diagnostic-20260909.bin`。两份合成音乐缓存与本轮测试生成数据逐字节相同，移除它们后重新打包，其余 17 个文件 SHA-256 全部一致，写回 Hash verified。没有删除旧 `/x.bin`、设置、旧页面或原完整备份。测试缓存可从诊断备份恢复。
- 清理并重新打包后，生产资源通过：Claude 159864 bytes、实际 Codex 机器人 172824 bytes、两个 logo 各 3200 bytes，以及天气文字、股票中文、天气页头/日期/空气资源全部 ACK。空间/文件系统状态影响了旧失败；尚未验证未来所有音乐与宠物组合的容量余量。
- `--test-live-device-data` exit 0：天气 26°C、自选股 6 条、Codex/国产额度/系统数据存在。本次 Claude 数据未就绪、音乐无会话，不据此宣称这些实时数据通过。测试改为单一资源发送者，避免后台和显式 ACK 检验重复发送及诊断竞争。
- 股票旧缓存再次像素核对 0/62400；宠物动画、迁移回归测试通过；Release 0 warnings/errors；diff --check 通过（仅已有 CRLF 提示）。
- 已重新启动新版桥接，未恢复旧版。股票实体字号、机器人外观及闪烁改善仍待用户看屏确认；Wi-Fi 回退和 macOS 仍未通过验收。
