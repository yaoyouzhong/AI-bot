# 展示顺序与监控数字刷新部署

- 展示顺序窗口改为可伸缩表格布局，底部保存/取消固定可见，移动按钮位于列表右侧；显示已选数量，选中与勾选分离，移动保留勾选状态。没有修改用户保存的页面顺序。
- 上下行文字数值保持 2 秒，采用最近 8 个网络样本的平均值；CPU/内存每 2 秒采集更新。原始网络曲线和样本序号仍按 250ms 推进，USB/LAN 与镜像复用同一组显示数值，不改协议或固件。
- Release 编译 0 警告/0 错误；布局在 1x/1.5x/2x 程序缩放检查通过（不等同于三种真实显示器 DPI），移动/勾选状态测试通过，100% 布局图已检查，测试没有保存设置。
- 设备回归：5 秒 20 帧、20 样本，静态框架与不变数字无重绘；9→100→9 恰好两次替换。
- 重启后 `/status` 实测：稳定阶段数字变化间隔约 1.98、1.92、2.11 秒，原始采样继续推进；系统页保持，GLM 数据仍可用。未刷写固件、未推送或发布。
