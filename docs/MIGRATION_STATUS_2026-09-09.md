# 持续迁移状态：2026-09-09

最新扩大审计：[全功能对齐审计](FULL_PARITY_AUDIT_2026-09-09.md)。25 个 Windows 新旧镜像场景零像素差异；全功能运行/实体屏/Mac 尚未闭环，详细未完成项以该表为准。

最新专项：[PRO 会员配色与股票名称对齐](PLAN_STOCK_PARITY_2026-09-09.md)。股票名称与运行中旧版位图逐像素比较为零差异；固件修改尚未刷入。

本页是当前入口，覆盖早期“本轮剩余已完成”等阶段性描述。目标仍是对应平台的旧版布局和行为，而不是近似替代。**当前尚不能宣布全部对齐或替换稳定版。**

最新真机结论：用户批准的 A45B02B 候选再次被反馈字体/颜色/宠物不一致，且 LAN 计数为零、Wi-Fi 回退失败；已恢复旧固件及旧桥接 PID 55568。详情见[当日第二次验收](HARDWARE_ACCEPTANCE_2026-09-09.md)。下方“未部署”和候选哈希是切换前历史快照，桥接后续只补强了验收脚本并重建，不能继续引用原 DLL 哈希作为最新候选。

## 本轮本地实现

| 范围 | 本轮修正 | 当前证据 |
| --- | --- | --- |
| Windows 托盘镜像 | 恢复 360×444 逻辑尺寸、288×288 镜像、八个模式按钮、亮度滑条和失焦隐藏；修正 175% DPI 裁切；亮度读取不覆盖用户新操作 | 窗口实际创建、按钮回调和 DrawToBitmap 预览通过；未访问设备 |
| 托盘操作 | 额度摘要、独立轮播开关/页面/间隔/顺序、天气动画旧名称、刷新状态、开机启动入口 | 菜单路由自测；没有启用启动项或替换运行程序 |
| 轮播和提醒 | 从启用时的第一页循环，手动选择停止轮播；注意/完成可临时覆盖固定页，屏保预览除外；红色 400ms 闪烁及恢复额度边框 | Windows 策略/图像测试；固件构建；Mac 同步源码未编译 |
| 音乐 | 保留无封面的 No Art / No Music；固件恢复封面 y14、文字区 y150、进度 y204、居中时间 y220；桌面保留自己旧版坐标 | Windows 无封面/封面自测；固件构建；真实媒体和设备未验收 |
| 系统监控 | Windows 最低纵轴 10240、峰值 8/7；固件恢复 5px 连续曲线、字号及坐标；不再使用错误的 1/2/5 整数档纵轴 | Windows 缩放断言和预览、固件构建；实机曲线/刷新待验收 |
| 自选股设置 | 保存后重新加载列表；请求中途改列表时丢弃旧响应，避免覆盖新设置 | Windows 构建与现有股票解析/设置回归；常驻使用待验收 |
| macOS | 左键弹出镜像/右键菜单、316×424 弹层、六个模式按钮/亮度；单额度按旧 Mac 的两行居中文字及额度边框，不套 Windows 套餐卡；音乐/股票/系统布局、250ms 系统采样与 USB metrics 源码 | 仅源码和新增 XCTest，不能在 Windows 编译 AppKit。用户确认有 Mac，之后验收 |
| Wi-Fi 诊断 | 回退失败时记录前/中/后 USB/LAN 计数、在线状态和是否取得 IP；不输出令牌、SSID、地址或页面内容 | 合成诊断与异常恢复测试通过；之前真实回退失败仍未闭环 |

天气、单/双额度、四家国产额度、桌宠原尺寸/私有素材导入和屏保的此前修正详见
[逐项布局记录](LAYOUT_AUDIT_2026-09-09.md)。这些是本地实现证据，不是逐像素或实体屏验收。

## 验证

- Windows Release：0 警告、0 错误。
- 13 组本地自测：display-policy、tray、mirror、settings、data、migration、codex-lifecycle、pet-animation、pet-selection、pet-gallery、migrated-domestic、usb-management、lan 全部退出 0。
- 固件 PlatformIO 构建通过：RAM 42116 / 81920，Flash 510959 / 1044464；仍有 TFT 触摸引脚未定义及既存日期格式警告。
- 图像：`artifacts/mirror-popup-self-test.png`、`mirror-self-test.png` 等均为新实现预览，不是旧屏截图。
- 额外实际系统采样自测通过；`--status-once` 可读取天气、6 只股票及国内外额度缓存。这不是账号重新授权或设备实时数据验收。
- 收尾只读检查：旧 `AIClockBridge` PID 56108 仍运行，旧 `/status` HTTP 200；没有新版常驻进程。
- 未刷写、未停止旧桥接、未更改开机启动、未推送、未打标签、未发布。

## 完成前仍须闭环

本地待验收候选（尚未部署，后续若重建须重新核对）：

- `firmware/.pio/build/nodemcuv2/firmware.bin` SHA-256：`A45B02B65D89D4C964636B791C81C04AE1547678A017B1A9082E4642B5360319`。
- `windows-app/AIBotBridge/bin/Release/net8.0-windows10.0.19041.0/AIBotBridge.dll` SHA-256：`EDFBA9B28C6EC6E0EAFA34CB80CF14F22965D4E6EC683CB859324D7B08F305E5`。以同目录 EXE 及依赖启动，不单独复制 DLL。

1. 对最终候选执行一次新版桥接与 ESP8266 的联合验收：所有页、真实天气/股票/额度、音乐、默认与自选桌宠、输入红框/完成绿环、屏保进入/唤醒；检查闪烁、资源更新及低内存回退。
2. 真实 8 秒 USB 超时 → LAN 状态/资源下载 → USB 恢复。先定位之前失败的阶段，不假定是网络隔离，不把合成测试当成修复证明。
3. 真实账户授权、供应商失败保留、改变自选股、亮度及重启后的设置；注册启动项的实际启用/关闭尚未执行。
4. Mac 上 `swift test && swift build -c release`，再验收菜单/弹窗/DPI、媒体授权、USB/LAN和缓存。Mac 源码不等于平台通过。旧 Mac 未包含 Windows 的国产额度授权采集，不能把通用活动页称为完整国产额度页。
5. 新旧同数据逐页复核，用户确认后才更新功能完成等级。当前仍保留旧版，切换验收需明确批准；公开推送/发布另行确认具体内容。
